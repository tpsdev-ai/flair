- **`secrets push` sends `processEnv: true` instead of `tier: "processEnv"` and verifies the row before reporting success.** Core's `set_secret` reads a boolean `processEnv` field, not a `tier` string — the old code silently landed every pushed secret in the inert state (processEnv: false). Added post-push read-back via `search_by_value` to confirm the row actually has processEnv set to true before marking the push as successful (flair#1105).
