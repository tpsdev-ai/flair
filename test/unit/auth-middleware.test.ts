import { describe, expect, test } from "bun:test";
import { setupHarperMocks } from "../helpers/mock-tables";

describe("auth middleware logic", () => {
  test("placeholder", () => {
    setupHarperMocks();
    expect(true).toBe(true);
  });
});
