{
  "mcpServers": {
    "flair": {
      "command": "npx",
      "args": ["-y", "@tpsdev-ai/flair-mcp"],
      "env": {
        "FLAIR_AGENT_ID": "${FLAIR_AGENT_ID}",
        "FLAIR_URL": "${FLAIR_URL}",
        "FLAIR_CLIENT": "cursor"
      }
    }
  }
}
