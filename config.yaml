name: flair
rest: true

http:
  port: 8787

graphqlSchema:
  files: schemas/*.graphql

jsResource:
  files: dist/resources/*.js

authentication:
  # Default secure. Set to false for local development only.
  authorizeLocal: true
  enableSessions: true
